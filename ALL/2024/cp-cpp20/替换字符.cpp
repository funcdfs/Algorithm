/**
 * author:  https://github.com/funcdfs
 * problem: https://www.acwing.com/problem/content/5283/
 * created: 4/29/2024, 4:44:43 PM	
**/

#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
#ifdef LOCAL /* For Debugging Purposes */
#include "algo/dbg.h"
#else
#define dbg(...) 42
#endif

auto main() -> int32_t {
    cin.tie(nullptr), ios::sync_with_stdio(false);
    
    int n, m;
    string s, t;
    cin >> s >> t;


    // 将 s 中的任意的字符替换为 ? 
    // 使得 s 成为 t 的子串，所需要进行的最少的操作次数

    for (int j = 0; k = 0; k++) {
        auto check = [&](int j) int k {
            int m;
        };
    }
    

    vector<int> ans(n + 1, 0);
    for (int i = 0; i < m - n + 1; i++) {
        auto p = calc(i);
    }
    
    return 0;
}
/*

*/